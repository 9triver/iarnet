# Docker Provider 配置
server:
  port: 50051  # gRPC 服务端口

docker:
  host: "tcp://dind-1:2375"  # Docker 引擎地址，连接到 dind-1 容器
  tls_cert_path: ""  # TLS 证书路径（可选，用于安全连接）
  tls_verify: false  # 是否启用 TLS 验证
  api_version: ""  # Docker API 版本（可选，留空使用版本协商）

resource:
  cpu: 8000 # 1000 millicores = 1 core
  memory: "8Gi"
  gpu: 4 # 4 GPUs

resource_tags:
 - cpu
 - memory
 - gpu
